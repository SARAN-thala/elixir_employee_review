<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below.</p>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :review, class: "control-label" %>
    <%= text_input f, :review, class: "form-control" %>
    <%= error_tag f, :review %>
  </div>

  <div class="form-group">
    <%= label f, :rating, class: "control-label" %>
    <%= text_input f, :rating, class: "form-control" %>
    <%= error_tag f, :rating %>
  </div>

  <div class="form-group">
    <%= label f, :employee_id, class: "control-label" %>
    <%= text_input f, :employee_id, class: "form-control" %>
    <%= error_tag f, :employee_id %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
